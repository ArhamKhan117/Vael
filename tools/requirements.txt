# Pinned exactly. The brand exports and the README art are committed output, so these are local
# authoring dependencies and not build or CI dependencies.
#
# One-time bootstrap from the repository root (this machine's python has no pip; a venv has one):
#   python3 -m venv tools/.venv
#   tools/.venv/bin/python -m pip install -r tools/requirements.txt
#
#   tools/.venv/bin/python tools/make_brand.py          # the logo master, the WebPs, the icons
#   tools/.venv/bin/python -m tools.make_readme_art     # the README hero and diagrams
Pillow==11.3.0
numpy==2.3.3
